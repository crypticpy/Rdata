% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-plotly.R
\name{apply_plotly_theme}
\alias{apply_plotly_theme}
\alias{plotly_dark_layout}
\alias{plotly_light_layout}
\title{Apply Theme-Aware Styling to Plotly Charts}
\usage{
apply_plotly_theme(p, dark_mode = TRUE)

plotly_dark_layout(p)

plotly_light_layout(p)
}
\arguments{
\item{p}{A plotly object created with \code{\link[plotly:plot_ly]{plotly::plot_ly()}} or
\code{\link[plotly:ggplotly]{plotly::ggplotly()}}.}

\item{dark_mode}{Logical. If \code{TRUE} (default), applies dark mode styling.
If \code{FALSE}, applies light mode styling suitable for print or light UIs.}
}
\value{
A modified plotly object with updated layout styling.
}
\description{
Applies consistent theming to plotly objects, matching the aesthetic of
\code{\link[=theme_worldclass]{theme_worldclass()}}. Handles background colors, fonts, axis styling,
and legend appearance for both dark and light modes.
}
\details{
The function modifies the following plotly layout properties:
\itemize{
\item \code{paper_bgcolor} and \code{plot_bgcolor} - Chart backgrounds
\item \code{font} - Global font settings
\item \code{legend} - Legend styling and positioning
\item \code{xaxis} and \code{yaxis} - Axis colors, gridlines, and tick formatting
}
}
\examples{
library(plotly)

# Create a basic plotly chart
p <- plot_ly(mtcars, x = ~wt, y = ~mpg, type = "scatter", mode = "markers")

# Apply dark mode theme (default)
p |> apply_plotly_theme()

# Apply light mode theme
p |> apply_plotly_theme(dark_mode = FALSE)

# Chain with other plotly modifications
p |>
  apply_plotly_theme() |>
  plotly::layout(title = "Weight vs MPG")

}
\seealso{
\code{\link[=theme_worldclass]{theme_worldclass()}} for ggplot2 theming
}
