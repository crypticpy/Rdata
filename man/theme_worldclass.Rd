% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-worldclass.R
\name{theme_worldclass}
\alias{theme_worldclass}
\alias{theme_wc}
\alias{theme_dashboard}
\title{World-Class ggplot2 Theme with Dark/Light Mode Support}
\usage{
theme_worldclass(dark_mode = TRUE, base_size = 14, base_family = "")

theme_wc(dark_mode = TRUE, base_size = 14, base_family = "")

theme_dashboard(dark_mode = TRUE, base_size = 14, base_family = "")
}
\arguments{
\item{dark_mode}{Logical. If \code{TRUE} (default), uses a dark color scheme
optimized for presentations and dashboards. If `}

\item{base_size}{Base font size in points. Default is 14.}

\item{base_family}{Base font family. Default uses system sans-serif.}
}
\value{
A ggplot2 theme object that can be added to any ggplot.
}
\description{
A polished, publication-ready ggplot2 theme designed for data communication
excellence. Features clean typography, minimal non-data ink, and full
support for both dark and light color schemes.
}
\details{
The theme follows key data visualization principles:
\itemize{
\item \strong{High data-ink ratio}: Minimal gridlines and borders
\item \strong{Clear typography}: Hierarchical text sizing for titles, labels
\item \strong{Accessibility}: Sufficient contrast ratios for readability
\item \strong{Flexibility}: Works on both light and dark backgrounds
}
}
\examples{
library(ggplot2)

# Basic usage with dark mode (default)
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_worldclass()

# Light mode for print/export
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_worldclass(dark_mode = FALSE)

# Customize base size
ggplot(iris, aes(Sepal.Length, Sepal.Width, color = Species)) +
  geom_point() +
  theme_worldclass(base_size = 16)

}
\seealso{
\code{\link[=apply_plotly_theme]{apply_plotly_theme()}} for theming plotly charts
}
